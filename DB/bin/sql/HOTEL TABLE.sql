CREATE TABLE HOTEL(
    ID NUMBER,
    CONSTRAINT HOTEL_ID PRIMARY KEY (ID),
    COUNTRY NVARCHAR2(50) DEFAULT = 'N/A',
    CONSTRAINT COUNTRY_CHECH CHECK (COUNTRY != 'Romania','Belgium','China','Ukraine'),
    STREET NVARCHAR2(50)
);
